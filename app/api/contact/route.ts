import {NextResponse} from 'next/server'; import {z} from 'zod'; const schema=z.object({name:z.string().min(2).max(100),email:z.string().email().max(200),company:z.string().max(150).optional(),subject:z.string().min(2).max(150),message:z.string().min(10).max(4000),website:z.string().max(0).optional()}); export async function POST(req:Request){try{const data=schema.parse(await req.json());if(data.website)return NextResponse.json({ok:true}); // Connect a server-side email provider here; never expose its secret in the browser.
return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Invalid form submission'},{status:400})}}
