import {products} from '@/config/products'; import {notFound} from 'next/navigation'; import type {Metadata} from 'next'; export function generateStaticParams(){return products.map(p=>({slug:p.slug}))} export function generateMetadata({params}:{params:{slug:string}}):Metadata{const p=products.find(x=>x.slug===params.slug);return {title:p?p.name:'Product'}} export default function Product({params}:{params:{slug:string}}){const p=products.find(x=>x.slug===params.slug);if(!p)notFound();return <main className="shell py-20"><p className="eyebrow">Digital product operated by Labs Dev Code</p><h1 className="display mt-3 text-5xl font-bold">{p.name}</h1><p className="mt-3 font-semibold text-blue">{p.category}</p><p className="mt-7 max-w-2xl text-lg leading-8 text-slate-600">{p.description}</p><div className="mt-12 grid gap-7 md:grid-cols-2"><div className="grid min-h-72 place-items-center rounded-2xl border border-line bg-slate-100"><div className="text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-blue text-xl font-bold text-white">{p.name.slice(-1)}</div><p className="mt-4 text-sm font-medium text-slate-500">Product preview</p></div></div><div><h2 className="text-2xl font-bold tracking-[-.04em]">Designed with business needs in mind</h2><ul className="mt-5 space-y-4">{p.benefits.map(b=><li className="flex gap-3 leading-6 text-slate-600" key={b}><span className="text-blue">●</span>{b}</li>)}</ul><a className="focus-ring mt-8 inline-block rounded-full bg-blue px-5 py-3 font-bold text-white" href={p.url} target="_blank" rel="noreferrer">Visit {p.name} →</a></div></div></main>}
