'use client'; import {useState} from 'react';
export function ContactForm(){const [state,setState]=useState<'idle'|'loading'|'success'|'error'>('idle');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('loading');setMsg('');const form=e.currentTarget;const res=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(res.ok){form.reset();setState('success');setMsg('Thank you. Your message has been received.')}else{setState('error');setMsg('We could not send your message. Please email info@labsdevcode.com.')}}return <form onSubmit={submit} className="card grid gap-5 p-6 sm:grid-cols-2" noValidate><input className="hidden" name="website" tabIndex={-1} autoComplete="off"/><Field label="Full Name" name="name" required/><Field label="Email" name="email" type="email" required/><Field label="Company" name="company"/><Field label="Subject" name="subject" required/><label className="grid gap-2 sm:col-span-2"><span className="text-sm font-semibold">Message</span><textarea required name="message" rows={5} className="focus-ring rounded-lg border border-line p-3"/></label><div className="sm:col-span-2"><button disabled={state==='loading'} className="focus-ring rounded-full bg-blue px-5 py-3 text-sm font-bold text-white disabled:opacity-60">{state==='loading'?'Sending…':'Send Message'}</button>{msg&&<p role="status" className={state==='error'?'mt-3 text-sm text-red-700':'mt-3 text-sm text-green-700'}>{msg}</p>}</div></form>}; function Field({label,name,type='text',required=false}:{label:string,name:string,type?:string,required?:boolean}){return <label className="grid gap-2"><span className="text-sm font-semibold">{label}</span><input required={required} type={type} name={name} className="focus-ring rounded-lg border border-line p-3"/></label>}
